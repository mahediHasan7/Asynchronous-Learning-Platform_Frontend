import './AsynchronousMainHeader.css';

const AsynchronousMainHeader = (props) => {
  return <header className="asynchronous-main-header">{props.children}</header>;
};

export default AsynchronousMainHeader;
